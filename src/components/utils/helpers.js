import axios from 'axios';
const api = require('./../../../server/routes/walmart-api');

const helpers = {
    getSearch: (query, brandName, results, startAt, sortedBy) =>{
        console.log('search helper is running');
    }
}